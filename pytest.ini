[pytest]
pythonpath = src
markers =
    requires_model: tests that require AI model downloads
    requires_api: tests that require external API access
    slow: tests that take a long time to run
    integration: integration tests
    unit: unit tests
